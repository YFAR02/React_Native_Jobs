import { View, Text} from 'react-native'; //view is like div and text is like a regular p tag

const Home = () => {
    return ( 
        <View> </View>
    )
}

export default Home; //exporting the component so it can be used in other files